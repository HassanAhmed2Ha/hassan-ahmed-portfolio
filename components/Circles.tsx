import React from "react";

// circles.png removed — returning null to eliminate Saturn rings from all sections
// Re-enable by restoring the Image below once a replacement decorative asset is ready
const Circles: React.FC = () => null;

export default Circles;
